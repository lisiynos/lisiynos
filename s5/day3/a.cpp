// Поиск максимального паросочетания (bipartile matching)

void dfs(int v, int color) {
  if(u[v] != 0)
    if(u[v] != color)
      ...
      else
        return 1;
  u[v] = color;
  for(i = 0;)
    if(g[v][i])
      dfs(i, 3 - )
    }
